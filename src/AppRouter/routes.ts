import { OrdersPage } from "./lazyPages";
import { RouteI } from "../types/interfaces/routes.interface";

const routes: RouteI[] = [
  {
    element: OrdersPage,
    path: "/",
  },
];

export default routes;
